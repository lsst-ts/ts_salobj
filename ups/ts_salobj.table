setupRequired(sconsUtils)
setupRequired(ts_ddsconfig)  # OpenSplice quality of service file
setupRequired(ts_xml)
setupRequired(ts_idl)  # enums from ts_xml
setupRequired(ts_utils)
setupOptional(ts_config_ocs)  # Test configuration files for unit tests
setupOptional(verify)

envPrepend(PATH, ${PRODUCT_DIR}/bin)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
